version: 2

models:
  - name: stg_orders
    description: one unique order per raw
    columns:
      - name: orderid
        description: primary key for stg_orders
      - name: shipmode
        description: '{{doc("shipmode")}}'

    